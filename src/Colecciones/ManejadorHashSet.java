package Colecciones;

public interface ManejadorHashSet<T> { //lo cambie a hashset porque en el array no puede haber pelis repetidas
    //reusado en Candy

    String mostrar();
    void agregar(T dato);

}
